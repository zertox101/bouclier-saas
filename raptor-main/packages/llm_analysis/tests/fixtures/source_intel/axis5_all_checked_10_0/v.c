extern char *kstrdup(const char *s, int gfp);
extern void use_string(const char *s);

void op_0(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_1(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_2(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_3(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_4(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_5(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_6(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_7(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_8(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

void op_9(const char *s) {
    char *p;
    p = kstrdup(s, 0);
    if (!p) return;
    use_string(p);
}

