import ShadowRootArsenal from '@/components/ShadowRootArsenal';

export const metadata = {
    title: 'Shadow Root Arsenal — Bouclier SOC',
    description: 'OAuth2 Intercept Module — Shadow DOM consent chain interception & authorization code capture.',
};

export default function ShadowArsenalPage() {
    return <ShadowRootArsenal />;
}
